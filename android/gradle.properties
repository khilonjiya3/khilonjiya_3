# ✅ UPDATED: Android Gradle Configuration for v2 embedding

# AndroidX migration
android.useAndroidX=true
android.enableJetifier=true

# Enable R8 full mode
android.enableR8.fullMode=true

# Kotlin multiplatform
kotlin.mpp.stability.nowarn=true

# Gradle JVM options
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# Gradle daemon
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.configureondemand=true
org.gradle.caching=true

# Flutter build mode (for release builds)
flutter.buildMode=release

# Enable ProGuard/R8 optimizations
android.enableR8=true

# Disable incremental annotation processing
kapt.incremental.apt=false

# Suppress Kotlin compiler warnings
kotlin.compiler.execution.strategy=in-process

# Android NDK (if needed)
android.ndkVersion=25.1.8937393